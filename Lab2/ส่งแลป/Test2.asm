;==== Test2.ASM "LONG" ====
	ORG	0000H
START:	MOV	DPTR,#0E060H
	MOV	A,#3FH	;Show 0
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#06H	;Show 1
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#5BH	;Show 2
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#4FH	;Show 3
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#66H	;Show 4
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#6DH	;Show 5
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#7DH	;Show 6
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#07H	;Show 7
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#7FH	;Show 8
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#6FH	;Show 9
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#77H	;Show A
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#7CH	;Show B
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#39H	;Show C
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#5EH	;Show D
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#79H	;Show E
	MOVX	@DPTR,A
	CALL	DELAY
	
	MOV	A,#71H	;Show F
	MOVX	@DPTR,A
	CALL	DELAY
	
DELAY:	MOV	R5,#04
DELAY1:	MOV	R6,#200
DELAY2:	MOV	R7,#200
DELAY3:	DJNZ	R7,DELAY3
	DJNZ	R6,DELAY2
	DJNZ	R5,DELAY1
	RET
	
	END